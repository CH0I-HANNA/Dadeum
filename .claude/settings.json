{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -iqE 'rm\\s+(-rf|-fr)|(curl|wget)\\s+.*\\|\\s*(ba)?sh|git\\s+push\\s+--force|git\\s+reset\\s+--hard|drop\\s+table'; then echo 'BLOCKED: 위험한 명령어가 감지되었습니다.' >&2; exit 2; fi"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -qF '.claude/settings.json'; then echo 'BLOCKED: .claude/settings.json은 에디터에서 직접 수정하세요.' >&2; exit 2; fi"
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -qF '.claude/settings.json'; then echo 'BLOCKED: .claude/settings.json은 에디터에서 직접 수정하세요.' >&2; exit 2; fi"
          }
        ]
      }
    ]
  }
}
